'use client'

import { SubscriptionType } from '@/app/main/(management)/subscriptions/types'

export default function OrganziationSubscriptionDetailView({
  close,
  data,
}: {
  close: () => void
  data: SubscriptionType
}) {
  return ''
}
